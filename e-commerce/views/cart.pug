
extends layout
include mixins/_productCardFromCart


block content
    .container
        h2 #{title}
        p
        .carts 
            each product in products  
                    each cart in carts  
                        if(product._id == cart.idProduct )
                            if(cart.user == user.email)
                             +productCardFromCart(product)
    
        p
    <div> 
        <h5><i> Purchase: </h5></i>
            each product in products  
                    each cart in carts  
                        if(product._id == cart.idProduct )
                            if(cart.user == user.email)
                                span <b>Product:</b> #{product.name}<br>
                                span <b>Price:</b> #{product.price}€<p>
                                span --------------------------------<p>
    </div>
    <div>
        <h2 style='padding-top:20px'><u> Payment                       
    </div>

    block scripts
    <script src="https://www.paypal.com/sdk/js?client-id=test"></script>
    <script>paypal.Buttons().render('body');</script>  
 
   
   


    
        
    
    
    
